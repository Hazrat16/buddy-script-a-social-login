# Copy to `apps/api/.env`

# PostgreSQL (matches root `docker-compose.yml` defaults)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/social_app?schema=public"

AUTH_SECRET="replace-with-openssl-rand-base64-32-min-chars"
API_PORT=3001
WEB_ORIGIN=http://localhost:3000
